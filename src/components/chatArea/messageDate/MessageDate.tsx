import "./MessageDate.css";

type MessageDateProps = {
  messageDate: string;
};

export const MessageDate = ({ messageDate }: MessageDateProps) => {
  return <div className="message-date"></div>;
};
